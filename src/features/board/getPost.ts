
import {listsBoardVO, listsResponseVO} from '@/entities/';

export const getPost = ():listsResponseVO => {
    const results:listsResponseVO = {
        pageCnt: 0,
        lists: []
    };
    return results;
}